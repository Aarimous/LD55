extends Node2D

@export var damage_text_packed : PackedScene

func create_damage_text(damage_amount, glob_pos):
	pass
	#var new_text : Label = damage_text_packed.instantiate()
	#add_child(new_text)
	#new_text.global_position = glob_pos
	#new_text.text = str(damage_amount)
	#new_text.init()
